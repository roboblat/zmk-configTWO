# Central-only (right half) settings.
#
# ZMK Studio runs on the CENTRAL half (the side that talks to the host over
# USB). Enabling it only here keeps the peripheral build small, since a
# Studio-enabled build needs significantly more RAM.
CONFIG_ZMK_STUDIO=y
